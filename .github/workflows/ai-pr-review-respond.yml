name: AI PR Review — Auto Fix

on:
  workflow_dispatch:
    inputs:
      pr_number:
        required: true
        type: string
      branch:
        required: true
        type: string
      fix_iter:
        required: true
        type: string
      prior_context:
        required: false
        type: string
        default: '[]'
  repository_dispatch:
    types: [pr-review-needs-fix]

jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    concurrency:
      group: ai-pr-review-fix-${{ github.event.inputs.pr_number || github.event.client_payload.pr_number }}
      cancel-in-progress: false

    permissions:
      actions: write
      contents: write
      pull-requests: write

    steps:
      - name: Validate client_payload inputs
        id: validate
        env:
          RAW_PR: ${{ github.event.inputs.pr_number || github.event.client_payload.pr_number }}
          RAW_BRANCH: ${{ github.event.inputs.branch || github.event.client_payload.branch }}
          RAW_ITER: ${{ github.event.inputs.fix_iter || github.event.client_payload.fix_iter }}
          RAW_PRIOR_CONTEXT: ${{ github.event.inputs.prior_context || github.event.client_payload.prior_context }}
        run: |
          if [[ ! "${RAW_PR}" =~ ^[0-9]+$ ]]; then
            echo "Invalid PR number: ${RAW_PR}" >&2
            exit 1
          fi
          if [[ ! "${RAW_BRANCH}" =~ ^[a-zA-Z0-9/_.-]+$ ]]; then
            echo "Unsafe branch name: ${RAW_BRANCH}" >&2
            exit 1
          fi
          if [[ ! "${RAW_ITER}" =~ ^[0-9]+$ ]]; then
            echo "Invalid fix_iter: ${RAW_ITER}" >&2
            exit 1
          fi
          echo "pr_number=${RAW_PR}" >> "$GITHUB_OUTPUT"
          echo "branch=${RAW_BRANCH}" >> "$GITHUB_OUTPUT"
          echo "fix_iter=${RAW_ITER}" >> "$GITHUB_OUTPUT"
          # Validate prior_context is a JSON array; fall back to empty array
          SAFE_PRIOR='[]'
          if printf '%s' "${RAW_PRIOR_CONTEXT}" | jq -e '. | arrays' >/dev/null 2>&1; then
            SAFE_PRIOR="${RAW_PRIOR_CONTEXT}"
          fi
          EOF_DELIM="PRIOR_EOF_$(openssl rand -hex 8)"
          echo "prior_context<<${EOF_DELIM}" >> "$GITHUB_OUTPUT"
          printf '%s' "${SAFE_PRIOR}" >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"
          echo "${EOF_DELIM}" >> "$GITHUB_OUTPUT"

      - name: Post working indicator
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
        run: |
          gh pr comment "${PR_NUMBER}" \
            --body "🔨 Applying AI fixes (iteration ${FIX_ITER})..." || true

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ steps.validate.outputs.branch }}
          fetch-depth: 0
          token: ${{ secrets.PR_CREATE_TOKEN }}

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '22'

      - uses: pnpm/action-setup@d15e628ca66d93ee5f352c71671a7bc6a97af5c9 # v6.4.0
        with:
          version: 9

      - name: Install web dependencies
        run: cd web && pnpm install --frozen-lockfile

      - name: Install OpenCode
        run: npm install -g @earendil-works/pi-coding-agent@0.75.5 pi-multiagent@0.9.4 pi-gh-cli@0.2.4 pi-lens@3.8.45

      - name: Configure git
        run: |
          git config --global user.name 'pi[bot]'
          git config --global user.email 'pi[bot]@users.noreply.github.com'

      - name: Setup OpenCode permissions
        run: |
          mkdir -p .pi/extensions/pi-permission-system
          echo '{"yoloMode": true}' > .pi/extensions/pi-permission-system/config.json

      - name: Add iteration label
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
        run: |
          LABEL="ai-fix-iter-${FIX_ITER}"
          gh label create "${LABEL}" \
            --color '0075ca' \
            --description "AI review fix iteration ${FIX_ITER}" \
            2>/dev/null || true
          gh pr edit "${PR_NUMBER}" --add-label "${LABEL}"

      - name: Mark findings in-progress
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
          REPO: ${{ github.repository }}
        run: |
          FINDING_IDS=$(gh api "repos/${REPO}/pulls/${PR_NUMBER}/comments" \
            --jq '[.[] | select(.user.login == "github-actions[bot]") | select(.body | test("[*][*](CRITICAL|HIGH) finding")) | .id]' \
            2>/dev/null || echo '[]')
          printf '%s' "$FINDING_IDS" | jq -r '.[]' 2>/dev/null | while read -r CID; do
            gh api "repos/${REPO}/pulls/comments/${CID}/reactions" \
              --method POST --field content='rocket' 2>/dev/null || true
            gh api "repos/${REPO}/pulls/comments/${CID}/replies" \
              --method POST \
              --field body="🔨 **Working on this** (fix iteration ${FIX_ITER})..." \
              2>/dev/null || true
          done

      - name: Run fix agent
        id: fix
        env:
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          BRANCH: ${{ steps.validate.outputs.branch }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
          PRIOR_CONTEXT: ${{ steps.validate.outputs.prior_context }}
        run: bash scripts/ai-pr-review-fix.sh

      - name: Push fixes
        id: push
        env:
          BRANCH: ${{ steps.validate.outputs.branch }}
        run: |
          if ! git diff --quiet HEAD; then
            git add -A
            git commit -m 'fix: apply AI review fixes'
          fi
          if git log "origin/${BRANCH}..HEAD" --oneline | grep -q .; then
            git push origin "${BRANCH}"
            echo 'pushed=true' >> "$GITHUB_OUTPUT"
          else
            echo 'pushed=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Run delta review
        if: steps.push.outputs.pushed == 'true'
        env:
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          BRANCH: ${{ steps.validate.outputs.branch }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
          PRIOR_CONTEXT: ${{ steps.validate.outputs.prior_context }}
          REPO: ${{ github.repository }}
        run: bash scripts/ai-pr-delta-review.sh

      - name: Post result
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
          PUSHED: ${{ steps.push.outputs.pushed }}
          FIX_OUTCOME: ${{ steps.fix.outcome }}
        run: |
          RETRY_CMD='`/ai Fix the remaining HIGH and CRITICAL findings from the AI code review`'
          if [[ "${PUSHED}" == 'true' ]]; then
            gh pr comment "${PR_NUMBER}" \
              --body "🔍 **Fix pushed (iteration ${FIX_ITER}) — delta review running.** Results will be posted to each finding thread." || true
          elif [[ "${FIX_OUTCOME}" == 'failure' ]]; then
            gh pr comment "${PR_NUMBER}" \
              --body "❌ **Fix agent failed (iteration ${FIX_ITER}).** To retry, post ${RETRY_CMD} as a PR comment." || true
          else
            gh pr comment "${PR_NUMBER}" \
              --body "⚠️ **Fix agent produced no changes (iteration ${FIX_ITER}).** To retry, post ${RETRY_CMD} as a PR comment." || true
          fi

      - name: Update finding comment status
        if: always() && steps.push.outputs.pushed != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.validate.outputs.pr_number }}
          FIX_ITER: ${{ steps.validate.outputs.fix_iter }}
          PUSHED: ${{ steps.push.outputs.pushed }}
          FIX_OUTCOME: ${{ steps.fix.outcome }}
          REPO: ${{ github.repository }}
        run: |
          FINDING_IDS=$(gh api "repos/${REPO}/pulls/${PR_NUMBER}/comments" \
            --jq '[.[] | select(.user.login == "github-actions[bot]") | select(.body | test("[*][*](CRITICAL|HIGH) finding")) | .id]' \
            2>/dev/null || echo '[]')
          COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo '')
          printf '%s' "$FINDING_IDS" | jq -r '.[]' 2>/dev/null | while read -r CID; do
            if [[ "${PUSHED}" == 'true' ]]; then
              MSG="✅ **Fixed** in commit \`${COMMIT}\` (iteration ${FIX_ITER}) — re-review running"
            elif [[ "${FIX_OUTCOME}" == 'failure' ]]; then
              MSG="❌ **Fix agent failed** (iteration ${FIX_ITER}) — manual intervention required"
            else
              MSG="⚠️ **No changes produced** (iteration ${FIX_ITER}) — will retry or escalate"
            fi
            gh api "repos/${REPO}/pulls/comments/${CID}/replies" \
              --method POST \
              --field body="$MSG" \
              2>/dev/null || true
          done
