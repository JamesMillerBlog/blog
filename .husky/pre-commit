pnpm --dir web precommit
pnpm typecheck
pnpm format:check

# Update documentation surgically if structural changes are staged.
# Skip with SKIP_DOCS_UPDATE=1
if [ -z "$SKIP_DOCS_UPDATE" ]; then
  ./scripts/update-docs.sh --precommit
fi
