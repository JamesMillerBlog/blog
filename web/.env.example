# S3 bucket containing MDX post files
# If not set, falls back to the local _posts/ directory (development only)
POSTS_BUCKET=your-posts-bucket-name
POSTS_BUCKET_REGION=us-east-1

# Optional: prefix/folder within the bucket (e.g. "posts/")
# Leave empty if MDX files are at the bucket root
POSTS_PREFIX=
